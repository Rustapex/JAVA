package object;

public class MethodSummary {
    public static void main(String[] args) {
        /*
[자세한 개념 설명]

1. static method
- static method는 객체가 아니라 클래스에 속한 메서드이다.
- 그래서 객체를 생성하지 않아도 "클래스이름.메서드이름()" 형태로 호출할 수 있다.
- static method는 "특정 객체 1개의 상태"를 기준으로 동작하는 것이 아니라,
  클래스 전체에 공통으로 필요한 기능을 담당할 때 사용한다.
- 예를 들면 계산기처럼 입력값만 받아서 결과를 반환하는 기능,
  공통 안내문 출력, 객체 개수 세기, 유효성 검사 같은 기능이 static에 잘 어울린다.
- static method 안에서는 instance variable(객체 변수)나 instance method를
  직접 사용할 수 없다.
- 이유는 static method가 실행되는 시점에는 "어느 객체를 기준으로 해야 하는지"
  정해져 있지 않기 때문이다.
- 즉 static method는 this를 사용할 수 없다.
- static method는 클래스가 메모리에 올라갈 때 함께 준비되며,
  객체를 만들기 전에도 사용할 수 있다.
- 대표적으로 main method가 static인 이유도 프로그램 시작 시점에
  아직 객체가 없기 때문이다.
- 정리하면 static method는 "객체 없이도 성립하는 공통 기능"에 붙인다.

2. instance method
- instance method는 객체에 속한 메서드이다.
- 객체를 생성한 뒤에 "참조변수.메서드이름()" 형태로 호출한다.
- instance method는 각 객체가 가진 고유한 데이터, 즉 instance variable을
  읽거나 수정하는 역할에 적합하다.
- 예를 들어 학생 객체의 이름, 점수, 상태를 변경하거나 출력하는 메서드는
  보통 instance method로 만든다.
- instance method는 호출한 객체를 기준으로 동작한다.
- 예를 들어 student1.printInfo()와 student2.printInfo()는
  같은 메서드를 호출하더라도 서로 다른 객체의 데이터를 사용한다.
- instance method 안에서는 instance variable, static variable,
  다른 instance method, static method 모두 사용할 수 있다.
- 왜냐하면 instance method는 "어떤 객체가 호출했는지"가 이미 정해져 있기 때문이다.
- 이때 현재 호출한 객체 자신을 가리키는 참조가 바로 this이다.
- 정리하면 instance method는 "객체마다 다른 상태를 다루는 기능"에 붙인다.

3. method
- method는 어떤 동작이나 기능을 하나의 이름으로 묶어 놓은 것이다.
- 입력을 받아서 처리하고, 필요하면 결과를 반환한다.
- 메서드를 사용하면 코드를 기능 단위로 분리할 수 있어서
  읽기 쉽고, 재사용하기 쉽고, 수정하기 쉬워진다.
- 같은 코드를 여러 번 반복해서 쓰지 않아도 되므로 유지보수성이 좋아진다.
- 메서드는 보통 "이 객체가 할 수 있는 행동" 또는
  "이 클래스가 제공하는 기능"을 표현한다.
- 객체지향에서는 데이터를 필드로 두고, 데이터를 다루는 동작을 메서드로 둔다.

4. method variable
- method variable이라는 표현은 보통 메서드 안에서 선언한 지역변수(local variable)를 말한다.
- 지역변수는 메서드가 실행될 때 만들어지고, 메서드가 끝나면 사라진다.
- 따라서 메서드 내부에서만 사용할 수 있다.
- 다른 메서드에서는 직접 접근할 수 없다.
- 예를 들어 반복문 인덱스 i, 임시 저장용 변수 temp, 계산 결과 sum 등이 지역변수이다.
- 지역변수는 해당 메서드의 작업을 수행하기 위한 임시 데이터라고 이해하면 된다.
- 지역변수는 자동으로 기본값이 들어가지 않으므로,
  사용하기 전에 반드시 직접 초기화해야 한다.

5. class variable
- class variable은 static variable이라고도 하며, 클래스에 속한 변수이다.
- 객체마다 따로 존재하는 것이 아니라, 모든 객체가 하나를 공유한다.
- 선언할 때 보통 static 키워드를 붙인다.
- 예를 들어 "전체 학생 수", "학교 이름", "공통 기준 점수"처럼
  객체가 달라도 모두 공통으로 가져야 하는 값은 class variable로 두기 좋다.
- 어떤 객체를 통해 접근하더라도 결국 같은 하나의 값을 바라본다.
- 그래서 한 객체에서 값을 변경하면 다른 객체에서도 그 변경 결과를 본다.
- class variable은 객체별 상태를 저장하는 용도가 아니라,
  클래스 전체의 공통 정보나 누적 정보 저장에 사용한다.

6. instance variable
- instance variable은 객체가 생성될 때 각 객체마다 따로 생기는 변수이다.
- 같은 클래스로 객체를 여러 개 만들면,
  instance variable도 객체 수만큼 각각 별도로 존재한다.
- 예를 들어 학생 클래스의 name, age, score 같은 값은
  학생마다 다를 수 있으므로 instance variable로 두는 것이 자연스럽다.
- 한 객체의 instance variable 값을 바꿔도 다른 객체의 값은 바뀌지 않는다.
- 보통 객체의 상태, 속성, 현재 값을 표현할 때 사용한다.

7. this
- this는 "현재 이 메서드를 호출한 객체 자신"을 가리키는 참조값이다.
- instance method나 생성자 안에서 사용할 수 있다.
- static method 안에서는 사용할 수 없다.
- this를 쓰는 가장 대표적인 이유는
  필드와 매개변수의 이름이 같을 때 구분하기 위해서이다.
- 예를 들어 this.name = name; 에서
  왼쪽 this.name은 객체의 필드이고,
  오른쪽 name은 메서드나 생성자의 매개변수이다.
- this를 사용하면 "현재 객체의 필드에 값을 넣는다"는 의미가 명확해진다.
- 또한 같은 객체의 다른 생성자를 호출할 때 this(...) 형태로 사용하기도 한다.
- this는 결국 "내가 지금 다루고 있는 객체가 누구인지"를 나타내는 키워드이다.

8. 생성자
- 생성자는 객체가 만들어질 때 자동으로 호출되는 특별한 메서드이다.
- 클래스 이름과 동일한 이름을 가진다.
- 반환형을 적지 않는다. void도 적지 않는다.
- 객체 생성 시점에 필드를 원하는 상태로 초기화하기 위해 사용한다.
- 예를 들어 이름, 점수, 가격, 수량 같은 값을 객체 생성과 동시에 넣어
  "완성된 상태의 객체"를 만들 수 있다.
- 생성자를 만들지 않으면 자바가 기본 생성자를 자동으로 제공할 수 있지만,
  개발자가 생성자를 하나라도 직접 만들면 기본 생성자가 자동으로 생기지 않을 수 있다.
- 생성자는 객체 생성 규칙을 강제하는 데에도 중요하다.
- 즉 "이 객체는 최소한 어떤 값들을 가져야 정상 상태인지"를 정하는 역할도 한다.
- 객체지향에서는 생성자를 잘 설계하는 것이 매우 중요하다.

9. public
- public은 접근 제어자(access modifier) 중 하나이다.
- public이 붙은 클래스, 변수, 메서드는 어디서든 접근할 수 있다.
- 다른 패키지에서도 접근 가능하다.
- 외부에 공개해도 되는 기능이나 반드시 사용해야 하는 기능에 붙인다.
- 예를 들어 객체를 사용하는 사람이 호출해야 하는 주요 메서드는
  public으로 두는 경우가 많다.
- 하지만 모든 것을 public으로 열어두면 객체 내부 데이터가 쉽게 변경되어
  캡슐화가 무너질 수 있다.
- 그래서 보통 필드는 private으로 숨기고,
  필요한 기능만 public method로 제공하는 방식이 많이 쓰인다.

10. 아무것도 안 적은 것
- 접근 제어자를 아무것도 적지 않으면 default access,
  또는 package-private 접근 제한이라고 한다.
- 이 경우 같은 패키지 안에서는 접근할 수 있지만,
  다른 패키지에서는 접근할 수 없다.
- 즉 public보다 범위가 좁고,
  private보다는 열려 있는 중간 정도의 접근 범위라고 볼 수 있다.
- 같은 패키지 내부에서만 사용할 클래스나 메서드에 둘 수 있다.
- "외부 패키지에는 굳이 공개하지 않지만, 같은 패키지 안에서는 함께 쓰겠다"
  같은 의도를 표현할 때 사용한다.
- 초보자 입장에서는 "아무것도 안 쓰면 그냥 다 되는 것"처럼 느껴질 수 있지만,
  실제로는 분명한 접근 제한 규칙이 존재한다.

11. public과 아무것도 안 적은 것의 차이
- public은 모든 패키지에서 접근 가능하다.
- 아무것도 안 적은 것은 같은 패키지 안에서만 접근 가능하다.
- 즉 범위는 public이 더 넓다.
- 외부에 공개할 API 성격이면 public,
  내부 패키지 구성원끼리만 쓸 기능이면 default(package-private)를 고려할 수 있다.

12. method와 variable을 객체지향적으로 보는 관점
- variable은 상태를 저장한다.
- method는 그 상태를 읽거나 바꾸는 행동을 담당한다.
- class variable은 클래스 전체의 공통 상태를 저장한다.
- instance variable은 객체 하나하나의 개별 상태를 저장한다.
- static method는 공통 동작을 담당한다.
- instance method는 개별 객체의 상태를 다루는 동작을 담당한다.
- 생성자는 객체가 처음 만들어질 때의 시작 상태를 정한다.
- this는 현재 객체를 가리킨다.
- public과 default는 외부에서 어디까지 접근 가능한지를 정한다.
- 결국 객체지향에서는 "어떤 데이터가 누구의 것인지",
  "어떤 기능이 누구 기준으로 동작하는지"를 구분하는 것이 핵심이다.
  */

/*


- static method : 클래스 소속 메서드, 객체 없이 호출 가능, this 사용 불가,
- instance method : 객체 소속 메서드, 객체 생성 후 호출, 객체별 상태 처리, this 사용 가능
- 핵심 기준 :
  객체마다 달라지는 데이터/기능 -> instance
  모두가 공유하는 데이터/기능 -> static
- this : 현재 메서드를 호출한 객체 자신을 가리키는 참조
- 생성자 : 객체 생성 시 자동 호출되는 메서드, 초기화 담당, 반환형 없음
( 생성자가 있어 빈 객체를 참조하는 메서드가 없게 한다.)
  */

/*
- method : 기능이나 동작을 묶은 코드 단위
- method variable(local variable) : 메서드 안에서만 쓰는 임시 변수, 메서드 종료 시 사라짐
- class variable(static variable) : 모든 객체가 공유하는 클래스 변수
- instance variable : 객체마다 따로 존재하는 변수

- public : 어디서든 접근 가능
- 아무것도 안 적은 것(default, package-private) : 같은 패키지 안에서만 접근 가능

*/

    }


}
